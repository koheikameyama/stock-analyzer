name: Daily Release

on:
  # 毎日UTC 11:00（日本時間 20:00）に自動実行
  schedule:
    - cron: '0 11 * * *'

  # 手動実行も可能
  workflow_dispatch:

jobs:
  create-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # リリース作成、タグ作成に必要
      pull-requests: read  # PR情報の読み取りに必要

    steps:
      - name: Check branch
        run: |
          # 手動実行の場合、developブランチからのみ実行可能
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "${{ github.ref }}" != "refs/heads/develop" ]; then
              echo "❌ エラー: このワークフローはdevelopブランチからのみ実行できます"
              echo "現在のブランチ: ${{ github.ref }}"
              exit 1
            fi
          fi

      - name: Checkout develop branch
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0  # 全履歴を取得（タグとコミット履歴取得のため）

      - name: Get last deployment tag
        id: last_deploy
        run: |
          # 最新のデプロイタグを取得（存在しない場合は最初のコミット）
          LAST_DEPLOY_TAG=$(git tag -l "deploy-*" --sort=-version:refname | head -n 1)

          if [ -z "$LAST_DEPLOY_TAG" ]; then
            # 初回デプロイの場合、最初のコミットから
            LAST_COMMIT=$(git rev-list --max-parents=0 HEAD)
            echo "last_commit=$LAST_COMMIT" >> $GITHUB_OUTPUT
            echo "is_first_deploy=true" >> $GITHUB_OUTPUT
          else
            LAST_COMMIT=$(git rev-list -n 1 $LAST_DEPLOY_TAG)
            echo "last_commit=$LAST_COMMIT" >> $GITHUB_OUTPUT
            echo "last_deploy_tag=$LAST_DEPLOY_TAG" >> $GITHUB_OUTPUT
            echo "is_first_deploy=false" >> $GITHUB_OUTPUT
          fi

          echo "Last deployment commit: $LAST_COMMIT"

      - name: Check if develop has changes
        id: check_changes
        run: |
          # developとmainの差分を確認
          git fetch origin main
          DIFF_COUNT=$(git rev-list --count origin/main..develop)

          if [ "$DIFF_COUNT" -eq 0 ]; then
            echo "No changes in develop since last deployment"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Found $DIFF_COUNT commits in develop"
          fi

      - name: Get info from existing deploy PR
        if: steps.check_changes.outputs.has_changes == 'true'
        id: version_check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # create-deploy-pr.ymlが作成したdevelop→mainのPRを取得
          DEPLOY_PR=$(gh pr list --head develop --base main --state open --json number,body --jq '.[0]')

          if [ -z "$DEPLOY_PR" ] || [ "$DEPLOY_PR" = "null" ]; then
            echo "No deploy PR found"
            echo "should_release=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          DEPLOY_PR_NUMBER=$(echo "$DEPLOY_PR" | jq -r '.number')
          DEPLOY_PR_BODY=$(echo "$DEPLOY_PR" | jq -r '.body')

          echo "Found deploy PR #$DEPLOY_PR_NUMBER"

          # PR本文から「含まれる変更」セクションを抽出
          PR_LIST=$(echo "$DEPLOY_PR_BODY" | awk '
            /^### 含まれる変更/ {flag=1; next}
            /^### / {flag=0}
            flag && /^- / {print}
          ')

          if [ -z "$PR_LIST" ]; then
            echo "No changes found in deploy PR"
            echo "should_release=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "PR List from deploy PR:"
          echo "$PR_LIST"

          # PRリストからバージョンラベルを検出
          HAS_MAJOR=false
          HAS_MINOR=false
          HAS_PATCH=false

          if echo "$PR_LIST" | grep -q "\[major\]"; then
            HAS_MAJOR=true
          fi
          if echo "$PR_LIST" | grep -q "\[minor\]"; then
            HAS_MINOR=true
          fi
          if echo "$PR_LIST" | grep -q "\[patch\]"; then
            HAS_PATCH=true
          fi

          # バージョンアップタイプを決定
          if [ "$HAS_MAJOR" = true ]; then
            BUMP_TYPE="major"
          elif [ "$HAS_MINOR" = true ]; then
            BUMP_TYPE="minor"
          elif [ "$HAS_PATCH" = true ]; then
            BUMP_TYPE="patch"
          else
            echo "No version labels found in PR list"
            echo "should_release=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "should_release=true" >> $GITHUB_OUTPUT
          echo "bump_type=$BUMP_TYPE" >> $GITHUB_OUTPUT
          echo "pr_list<<EOF" >> $GITHUB_OUTPUT
          echo "$PR_LIST" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "Version bump type: $BUMP_TYPE"

      - name: Calculate new version
        if: steps.version_check.outputs.should_release == 'true'
        id: new_version
        run: |
          # 最新のバージョンタグを取得
          LATEST_TAG=$(git describe --tags --abbrev=0 --match "v*.*.*" 2>/dev/null || echo "v0.0.0")
          echo "Latest version tag: $LATEST_TAG"

          # vを除いた数値部分を抽出
          VERSION=${LATEST_TAG#v}
          IFS='.' read -ra VERSION_PARTS <<< "$VERSION"
          MAJOR=${VERSION_PARTS[0]:-0}
          MINOR=${VERSION_PARTS[1]:-0}
          PATCH=${VERSION_PARTS[2]:-0}

          echo "Current version: $MAJOR.$MINOR.$PATCH"

          # バージョンをインクリメント
          BUMP_TYPE="${{ steps.version_check.outputs.bump_type }}"
          if [ "$BUMP_TYPE" = "major" ]; then
            MAJOR=$((MAJOR + 1))
            MINOR=0
            PATCH=0
          elif [ "$BUMP_TYPE" = "minor" ]; then
            MINOR=$((MINOR + 1))
            PATCH=0
          elif [ "$BUMP_TYPE" = "patch" ]; then
            PATCH=$((PATCH + 1))
          fi

          NEW_VERSION="v$MAJOR.$MINOR.$PATCH"
          echo "New version: $NEW_VERSION"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Create release notes
        if: steps.version_check.outputs.should_release == 'true'
        id: release_notes
        run: |
          PR_LIST="${{ steps.version_check.outputs.pr_list }}"

          # PRリストをそのままリリースノートとして使用
          RELEASE_NOTES="## 更新内容\n\n$PR_LIST"

          # 複数行の出力を正しく扱う
          echo "release_notes<<EOF" >> $GITHUB_OUTPUT
          echo -e "$RELEASE_NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Checkout main branch
        if: steps.check_changes.outputs.has_changes == 'true' && steps.version_check.outputs.should_release == 'true'
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Create GitHub Release
        if: steps.check_changes.outputs.has_changes == 'true' && steps.version_check.outputs.should_release == 'true'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new_version }}"
          RELEASE_NOTES="${{ steps.release_notes.outputs.release_notes }}"

          gh release create "$NEW_VERSION" \
            --title "$NEW_VERSION" \
            --notes "$RELEASE_NOTES" \
            --target main

          echo "✅ GitHub Release created: $NEW_VERSION"

      - name: Summary
        if: always()
        run: |
          echo "### 🚀 Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.check_changes.outputs.has_changes }}" == "false" ]; then
            echo "ℹ️ **No Changes**: develop has no changes since last deployment" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          if [ "${{ steps.version_check.outputs.should_release }}" == "true" ]; then
            echo "📦 **Release Created**: ${{ steps.new_version.outputs.new_version }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Type**: ${{ steps.version_check.outputs.bump_type }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Included PRs" >> $GITHUB_STEP_SUMMARY
            echo "${{ steps.version_check.outputs.pr_list }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ **No Release**: No version labels found in deploy PR" >> $GITHUB_STEP_SUMMARY
            echo "- PRは作成されていますが、version:*ラベルが付いていないため、リリースは作成されませんでした" >> $GITHUB_STEP_SUMMARY
          fi
