name: Daily Merge and Release

on:
  # 毎日UTC 11:00（日本時間 20:00）に自動実行
  schedule:
    - cron: '0 11 * * *'

  # 手動実行も可能
  workflow_dispatch:

jobs:
  merge-and-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # リリース作成、タグ作成、PRマージに必要
      pull-requests: write  # PR作成に必要

    steps:
      - name: Check branch
        run: |
          # 手動実行の場合、developブランチからのみ実行可能
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            if [ "${{ github.ref }}" != "refs/heads/develop" ]; then
              echo "❌ エラー: このワークフローはdevelopブランチからのみ実行できます"
              echo "現在のブランチ: ${{ github.ref }}"
              exit 1
            fi
          fi

      - name: Checkout develop branch
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0  # 全履歴を取得（タグとコミット履歴取得のため）

      - name: Get last deployment tag
        id: last_deploy
        run: |
          # 最新のデプロイタグを取得（存在しない場合は最初のコミット）
          LAST_DEPLOY_TAG=$(git tag -l "deploy-*" --sort=-version:refname | head -n 1)

          if [ -z "$LAST_DEPLOY_TAG" ]; then
            # 初回デプロイの場合、最初のコミットから
            LAST_COMMIT=$(git rev-list --max-parents=0 HEAD)
            echo "last_commit=$LAST_COMMIT" >> $GITHUB_OUTPUT
            echo "is_first_deploy=true" >> $GITHUB_OUTPUT
          else
            LAST_COMMIT=$(git rev-list -n 1 $LAST_DEPLOY_TAG)
            echo "last_commit=$LAST_COMMIT" >> $GITHUB_OUTPUT
            echo "last_deploy_tag=$LAST_DEPLOY_TAG" >> $GITHUB_OUTPUT
            echo "is_first_deploy=false" >> $GITHUB_OUTPUT
          fi

          echo "Last deployment commit: $LAST_COMMIT"

      - name: Check if develop has changes
        id: check_changes
        run: |
          # developとmainの差分を確認
          git fetch origin main
          DIFF_COUNT=$(git rev-list --count origin/main..develop)

          if [ "$DIFF_COUNT" -eq 0 ]; then
            echo "No changes in develop since last deployment"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "Found $DIFF_COUNT commits in develop"
          fi

      - name: Collect merged PRs and determine version bump
        if: steps.check_changes.outputs.has_changes == 'true'
        id: version_check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # mainとdevelopの差分からマージコミットを取得
          MERGE_COMMITS=$(git log origin/main..develop --merges --pretty=format:"%H %s" | grep "Merge pull request" || true)

          if [ -z "$MERGE_COMMITS" ]; then
            echo "No PRs merged since last deployment"
            echo "should_release=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Merged PRs since last deployment:"
          echo "$MERGE_COMMITS"

          # PRナンバーを抽出してラベルを取得
          HAS_MAJOR=false
          HAS_MINOR=false
          HAS_PATCH=false
          PR_LIST=""

          while read -r commit_hash commit_msg; do
            PR_NUMBER=$(echo "$commit_msg" | grep -oP '#\K\d+' || true)
            if [ -n "$PR_NUMBER" ]; then
              echo "Checking PR #$PR_NUMBER..."
              LABELS=$(gh pr view $PR_NUMBER --json labels --jq '.labels[].name' || true)

              if echo "$LABELS" | grep -q "version:major"; then
                HAS_MAJOR=true
              elif echo "$LABELS" | grep -q "version:minor"; then
                HAS_MINOR=true
              elif echo "$LABELS" | grep -q "version:patch"; then
                HAS_PATCH=true
              fi

              PR_LIST="$PR_LIST #$PR_NUMBER"
            fi
          done <<< "$MERGE_COMMITS"

          # バージョンアップタイプを決定
          if [ "$HAS_MAJOR" = true ]; then
            BUMP_TYPE="major"
          elif [ "$HAS_MINOR" = true ]; then
            BUMP_TYPE="minor"
          elif [ "$HAS_PATCH" = true ]; then
            BUMP_TYPE="patch"
          else
            echo "No version labels found in merged PRs"
            echo "should_release=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "should_release=true" >> $GITHUB_OUTPUT
          echo "bump_type=$BUMP_TYPE" >> $GITHUB_OUTPUT
          echo "pr_list=$PR_LIST" >> $GITHUB_OUTPUT
          echo "Version bump type: $BUMP_TYPE"
          echo "Related PRs: $PR_LIST"

      - name: Calculate new version
        if: steps.version_check.outputs.should_release == 'true'
        id: new_version
        run: |
          # 最新のバージョンタグを取得
          LATEST_TAG=$(git describe --tags --abbrev=0 --match "v*.*.*" 2>/dev/null || echo "v0.0.0")
          echo "Latest version tag: $LATEST_TAG"

          # vを除いた数値部分を抽出
          VERSION=${LATEST_TAG#v}
          IFS='.' read -ra VERSION_PARTS <<< "$VERSION"
          MAJOR=${VERSION_PARTS[0]:-0}
          MINOR=${VERSION_PARTS[1]:-0}
          PATCH=${VERSION_PARTS[2]:-0}

          echo "Current version: $MAJOR.$MINOR.$PATCH"

          # バージョンをインクリメント
          BUMP_TYPE="${{ steps.version_check.outputs.bump_type }}"
          if [ "$BUMP_TYPE" = "major" ]; then
            MAJOR=$((MAJOR + 1))
            MINOR=0
            PATCH=0
          elif [ "$BUMP_TYPE" = "minor" ]; then
            MINOR=$((MINOR + 1))
            PATCH=0
          elif [ "$BUMP_TYPE" = "patch" ]; then
            PATCH=$((PATCH + 1))
          fi

          NEW_VERSION="v$MAJOR.$MINOR.$PATCH"
          echo "New version: $NEW_VERSION"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Create release notes
        if: steps.version_check.outputs.should_release == 'true'
        id: release_notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LAST_COMMIT="${{ steps.last_deploy.outputs.last_commit }}"
          PR_LIST="${{ steps.version_check.outputs.pr_list }}"

          # リリースノート作成
          RELEASE_NOTES="## 更新内容\n\n"

          for PR_NUMBER in $PR_LIST; do
            PR_NUMBER=${PR_NUMBER#\#}
            PR_INFO=$(gh pr view $PR_NUMBER --json title,body,labels)
            PR_TITLE=$(echo "$PR_INFO" | jq -r '.title')
            PR_BODY=$(echo "$PR_INFO" | jq -r '.body')

            # PR本文から「リリースノート（ユーザー向け）」セクションを抽出
            USER_NOTES=$(echo "$PR_BODY" | awk '
              /^## リリースノート（ユーザー向け）/ {flag=1; next}
              /^## / {flag=0}
              flag && /^[^<]/ {print}
            ' | sed '/^$/d' | sed '/^-$/d')

            if [ -n "$USER_NOTES" ]; then
              RELEASE_NOTES="${RELEASE_NOTES}${USER_NOTES}\n"
            else
              RELEASE_NOTES="${RELEASE_NOTES}- ${PR_TITLE} (#${PR_NUMBER})\n"
            fi
          done

          # 複数行の出力を正しく扱う
          echo "release_notes<<EOF" >> $GITHUB_OUTPUT
          echo -e "$RELEASE_NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create and merge PR from develop to main
        if: steps.check_changes.outputs.has_changes == 'true'
        id: merge_pr
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          PR_LIST: ${{ steps.version_check.outputs.pr_list }}
          SHOULD_RELEASE: ${{ steps.version_check.outputs.should_release }}
          NEW_VERSION: ${{ steps.new_version.outputs.new_version }}
          BUMP_TYPE: ${{ steps.version_check.outputs.bump_type }}
        run: |
          # 既存のPRをチェック
          EXISTING_PR=$(gh pr list --head develop --base main --state open --json number --jq '.[0].number' || echo "")

          # PR本文を生成
          PR_TITLE="chore: Daily deployment $(date -u +%Y-%m-%d)"

          if [ "$SHOULD_RELEASE" = "true" ]; then
            VERSION_SECTION="### バージョン
          - **Version**: $NEW_VERSION
          - **Type**: $BUMP_TYPE"
          else
            VERSION_SECTION="### バージョン
          - リリースなし（version:*ラベルなし）"
          fi

          PR_BODY="## 自動デプロイPR

          このPRは毎日20:00に自動作成されます。

          ### 含まれる変更
          $PR_LIST

          $VERSION_SECTION

          🤖 This PR is automatically generated by GitHub Actions"

          if [ -n "$EXISTING_PR" ]; then
            # 既存PRを更新
            echo "Updating existing PR #$EXISTING_PR"
            gh pr edit $EXISTING_PR --title "$PR_TITLE" --body "$PR_BODY"
            PR_NUMBER=$EXISTING_PR
            PR_URL="https://github.com/${{ github.repository }}/pull/$EXISTING_PR"
            echo "Updated PR #$EXISTING_PR: $PR_URL"
          else
            # 新規PR作成
            echo "Creating new PR"
            PR_URL=$(gh pr create \
              --base main \
              --head develop \
              --title "$PR_TITLE" \
              --body "$PR_BODY")
            PR_NUMBER=$(echo "$PR_URL" | grep -oP '\d+$')
            echo "Created PR #$PR_NUMBER: $PR_URL"
          fi

          echo "pr_url=$PR_URL" >> $GITHUB_OUTPUT
          echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT

          # PRを自動マージ
          gh pr merge $PR_NUMBER --merge --auto
          echo "Set auto-merge for PR #$PR_NUMBER"

      - name: Checkout main branch
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Create GitHub Release
        if: steps.check_changes.outputs.has_changes == 'true' && steps.version_check.outputs.should_release == 'true'
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new_version }}"
          RELEASE_NOTES="${{ steps.release_notes.outputs.release_notes }}"

          gh release create "$NEW_VERSION" \
            --title "$NEW_VERSION" \
            --notes "$RELEASE_NOTES" \
            --target main

          echo "✅ GitHub Release created: $NEW_VERSION"

      - name: Summary
        if: always()
        run: |
          echo "### 🚀 Merge and Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.check_changes.outputs.has_changes }}" == "false" ]; then
            echo "ℹ️ **No Changes**: develop has no changes since last merge" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          if [ -n "${{ steps.merge_pr.outputs.pr_url }}" ]; then
            echo "✅ **develop → main merged**: ${{ steps.merge_pr.outputs.pr_url }}" >> $GITHUB_STEP_SUMMARY
            echo "- Railway will automatically deploy from main branch" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.version_check.outputs.should_release }}" == "true" ]; then
            echo "📦 **Release Created**: ${{ steps.new_version.outputs.new_version }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Type**: ${{ steps.version_check.outputs.bump_type }}" >> $GITHUB_STEP_SUMMARY
            echo "- **PRs**: ${{ steps.version_check.outputs.pr_list }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ **No Release**: No version labels in merged PRs" >> $GITHUB_STEP_SUMMARY
          fi
